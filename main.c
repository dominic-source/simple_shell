#include "main.h"

/**
 * main - start a simple shell
 * @ac: count of argument
 * @av: arguments
 * Return: -1 or 0
 */
int main (int ac, char *av[])
{

	size_t len = 0;
	ssize_t line;

	getline(lptr, len, stdin);

	return (0);
}
